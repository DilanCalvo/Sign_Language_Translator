# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Large raw datasets (regenerated by capturing your own signs)
data/asl_alphabet/
data/asl_words/
data/processed/

# IDE
.vscode/
.idea/
*.code-workspace

# Operating system
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Internal context file (not part of the public project)
CLAUDE.md

# NOTE: trained models (model/*.h5, model/*.task), label maps (model/*.json)
# and the captured sample CSVs (data/real_capture/) ARE committed on purpose,
# so the app runs right after cloning. They are small (< 15 MB total).
